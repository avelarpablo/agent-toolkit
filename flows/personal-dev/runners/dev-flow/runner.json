{
  "entrypoint": "dev-flow",
  "description": "Deterministic pipeline mechanics the orchestrator calls — worktree create/teardown/seed, stage-label flips, and merge-keyed cleanup. No judgment; the orchestrator owns policy."
}
